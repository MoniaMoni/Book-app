<p id="notice"><%= notice %></p>

<br>
<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h4 class="card-title"><strong><%= @book.title %></strong></h5>
    <h5 class="card-subtitle mb-2 text-muted"><%= @book.author %></h6>
    <p class="h6 card-text"><%= @book.description %></p>
    <a href="#" class="card-link"></a>
  </div>
</div>

</br>

<%= link_to 'Edit', edit_book_path(@book), class:"btn btn-outline-dark" %>
<%= link_to 'Back', books_path, class:"btn btn-outline-dark" %>
